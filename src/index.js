//create an app
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//render the app
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
